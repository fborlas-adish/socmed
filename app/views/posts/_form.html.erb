<%= form_with model: user_post do |form| %>
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>

  <%= form.file_field :media, multiple: true %> 

  <p>
    <%= form.submit %>
  </p>
<% end %>